# Native-window launcher dependencies, kept out of requirements.txt so
# server and CI installs stay unchanged.
pywebview==6.2.1
# 3.50.x is the last PyGObject series that builds against girepository-1.0,
# which matches Ubuntu 24.04's libgirepository1.0-dev.
PyGObject==3.50.1 ; sys_platform == "linux"
